spring:
  cloud:
    config:
      # config service uri
      uri: http://127.0.0.1:6400
      profile: dev
      name: book
      label: main

  kafka:
    bootstrap-servers:
      - 47.111.21.139:9092
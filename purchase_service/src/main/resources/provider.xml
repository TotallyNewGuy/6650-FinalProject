<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd
           http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 指定当前服务的名字（同样的服务名字相同，不要和别的服务同名） -->
    <dubbo:application name="purchase-service"/>

    <!-- 指定注册中心的位置 -->
    <dubbo:registry address="zookeeper://47.111.21.139:2181"/>

    <!-- 指定通信规则（通信协议和通信端口） -->
    <dubbo:protocol name="dubbo" port="20884"/>

    <!-- 发现（使用）服务 -->
    <dubbo:reference interface="com.project.api.StockService" id="stockService" timeout="100000"/>
    <dubbo:reference interface="com.project.api.BookService" id="bookService" timeout="100000"/>
    <dubbo:reference interface="com.project.api.OrderService" id="orderService" timeout="100000"/>
    <dubbo:reference interface="com.project.api.BalanceService" id="balanceService" timeout="100000"/>

    <!-- 指定监控中心，registry意为通过注册中心自动寻找监控中心 -->
<!--    <dubbo:monitor protocol="registry"/>-->
</beans>
